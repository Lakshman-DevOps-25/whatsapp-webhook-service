{
  "name": "whatsapp-webhook-service",
  "version": "1.0.0",
  "description": "Production-ready WhatsApp Business webhook service: config + token lifecycle, inbound/outbound messages and media, persisted to MongoDB and MinIO.",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js"
  },
  "engines": { "node": ">=20" },
  "license": "MIT",
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "minio": "^8.0.1",
    "mongoose": "^8.5.1",
    "pino": "^9.3.2",
    "pino-http": "^10.2.0"
  }
}
